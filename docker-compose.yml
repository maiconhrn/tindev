version: "3.7"
services:
  backend:
    build: "./backend"
    container_name: node-container
    restart: always
    ports:
      - "3333:3333"
  webapp:
    build: "./webapp"
    container_name: react-container
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - backend
